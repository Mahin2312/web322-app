<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Mahin Ibne Alam's Store</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous" />
    <link rel="stylesheet" type="text/css" href="/css/main.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">WEB322 - Mahin Ibne Alam</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    {{#navLink "/about"}}About{{/navLink}}
                    {{#navLink "/items"}}Items{{/navLink}}
                    {{#navLink "/shop"}}Shop{{/navLink}}
                    {{#navLink "/categories"}}Categories{{/navLink}}
                </ul>

                <!-- Conditionally rendered links based on session.user -->
                {{#if session.user}}
                    <!-- If user is logged in -->
                    <form class="navbar-form navbar-right">
                        <div class="dropdown">
                            <button class="dropdown-toggle" type="button">
                                <span class="bi bi-person"></span>&nbsp;&nbsp;{{session.user.userName}}&nbsp;&nbsp;
                            </button>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="/userHistory">User History</a>
                                <a class="dropdown-item" href="/logout">Log Out</a>
                            </div>
                        </div>
                    </form>
                {{else}}
                    <!-- If user is NOT logged in -->
                    <form class="navbar-form navbar-right">
                        <a href="/register" class="btn btn-success"><span class="bi bi-cog"></span>&nbsp;&nbsp;Register</a>
                        <a href="/login" class="btn btn-primary"><span class="bi bi-chevron-right"></span>&nbsp;&nbsp;Log In</a>
                    </form>
                {{/if}}
            </div>
        </div>
    </nav>

    <div class="container">
        {{{body}}}
    </div>

</body>
</html>
